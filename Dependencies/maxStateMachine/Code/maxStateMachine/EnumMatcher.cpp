#include <maxStateMachine/EnumMatcher.hpp>

namespace maxStateMachine {

	/*
	constexpr bool EnumMatcher::DoesMatch(const Input& input) noexcept {
		return true;
	}
	*/

} // namespace maxStateMachine